/**
 * 
 * Данный компонент через props получает информацию по текущему курсу валют
 * и формирует элемент разметки для ее отображения.
 * 
 */
 

 export default function rate(props) {
 	return(<div></div>);
 }