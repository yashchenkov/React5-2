/**
 * Компонент отвечает за формирование списка новостей.
 * Каждая новость - это новый блок, который получает информацию от родительского(этого)
 * компонента через props
 * 
 */


export default function news() {
  return(<div></div>);
}