/**
 * 
 * Данный компонент является полностью независимым и автономным.
 * Сюда ничего не передается, компонент только отправляет запрос на сервер.
 * 
 */

export default function search() {
  return(<div></div>);
}