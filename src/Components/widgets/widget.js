/**
 * 
 * В зависимости от возвращаегомого типа формирует элемент,
 * который отображает соответствующую информацию посредством метода условного рендеринга.
 * Всю информацию получает через props.
 * 
 */

export default function widget(props) {
	return(<div></div>);
}